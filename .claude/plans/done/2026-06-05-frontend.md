# アーキテクチャ再設計 — Phase 2: Frontend 実装計画

> **エージェント向け:** REQUIRED SUB-SKILL: **`subagent-driven-development`** を使ってこの計画をタスクごとに実装すること。`executing-plans` は使用禁止。ステップはチェックボックス構文（`- [ ]`）で追跡。

**目標:** Supabase Auth 連携・Atomic Design 導入・タップ撮影 UX・みんなの履歴・サブスク情報表示を構築する。

**前提:** `2026-06-05-foundation.md` の Phase 1 が完了していること。

**アーキテクチャ:** Supabase Auth（JWT 発行） → Next.js Middleware（ルート保護） → TanStack Query（状態管理） → Atomic Design（UI 構成）。API 呼び出し時は `Authorization: Bearer <token>` ヘッダーを付与する。

**技術スタック:** Next.js 16 / TanStack Query v5（既存）/ @supabase/supabase-js / shadcn/ui / Tailwind v4 / next-intl（既存）

**仕様書:** `docs/specs/2026-06-05-architecture-redesign.md` Section 2, 5, 6

---

### Task 1: パッケージインストール + セットアップ

**Files:**
- Modify: `frontend/package.json`
- Create: `frontend/src/lib/supabase/client.ts`
- Create: `frontend/src/lib/supabase/server.ts`
- Modify: `frontend/src/lib/queryClient.ts`（新規作成）

- [ ] **Step 1: Supabase クライアントをインストール**

```bash
cd frontend
pnpm add @supabase/supabase-js
```

- [ ] **Step 2: shadcn/ui を初期化**

```bash
cd frontend
pnpm dlx shadcn@latest init
```

プロンプトで選択:
- Style: Default
- Base color: Slate
- CSS variables: Yes

最低限インストールするコンポーネント:

```bash
pnpm dlx shadcn@latest add button badge skeleton dialog sheet input label
```

- [ ] **Step 3: 環境変数を設定**

`frontend/.env.local`（gitignore 済み）に追加:

```bash
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
```

- [ ] **Step 4: Supabase クライアントを作成**

`frontend/src/lib/supabase/client.ts`:

```typescript
import { createBrowserClient } from '@supabase/ssr'

export const createClient = () =>
  createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
  )
```

`frontend/src/lib/supabase/server.ts`（Server Components / Middleware 用）:

```typescript
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export const createServerClient = async () => {
  const cookieStore = await cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll: () => cookieStore.getAll(),
        setAll: (cookiesToSet) => {
          cookiesToSet.forEach(({ name, value, options }) =>
            cookieStore.set(name, value, options),
          )
        },
      },
    },
  )
}
```

**Note:** `@supabase/ssr` を別途インストール:

```bash
pnpm add @supabase/ssr
```

- [ ] **Step 5: API クライアントのベース関数を更新**

`frontend/src/lib/api/api-client.ts` を新規作成（Bearer Token 自動付与）:

```typescript
import { createClient } from '@/lib/supabase/client'

export const apiFetch = async (path: string, init?: RequestInit): Promise<Response> => {
  const supabase = createClient()
  const { data: { session } } = await supabase.auth.getSession()
  const token = session?.access_token

  const headers: HeadersInit = {
    'Content-Type': 'application/json',
    ...(token ? { Authorization: `Bearer ${token}` } : {}),
    ...init?.headers,
  }

  const baseUrl = process.env.NEXT_PUBLIC_API_BASE_URL ?? ''
  const res = await fetch(`${baseUrl}${path}`, { ...init, headers })

  if (!res.ok) {
    const body = await res.json().catch(() => ({}))
    throw Object.assign(new Error(body.message ?? 'API error'), { status: res.status, body })
  }
  return res
}
```

既存の `users.api.ts`・`history.api.ts`・`scan.api.ts`・`allergens.api.ts` を `apiFetch` を使うように更新する（Cookie 送信の `credentials: 'include'` は削除）。

- [ ] **Step 6: TanStack Query の Provider を作成**

`frontend/src/lib/queryClient.ts`:

```typescript
import { QueryClient } from '@tanstack/react-query'

export const createQueryClient = () =>
  new QueryClient({
    defaultOptions: {
      queries: {
        staleTime: 30 * 1000,
        retry: 1,
      },
    },
  })
```

- [ ] **Step 7: コミット**

```bash
git add frontend/
git commit -m "feat(frontend): install @supabase/supabase-js, shadcn/ui, update api-client"
```

---

### Task 2: Atomic Design ディレクトリ構成 + 共通 atoms

**Files:**
- Create: `frontend/src/components/atoms/LoadingOverlay.tsx`
- Create: `frontend/src/components/atoms/LoadingOverlay.test.tsx`
- Create: `frontend/src/components/atoms/LoadingSpinner.tsx`
- Create: `frontend/src/components/molecules/JudgmentBadge.tsx`
- Create: `frontend/src/components/molecules/JudgmentBadge.test.tsx`
- Create: `frontend/src/components/molecules/ScanLimitBadge.tsx`

Atomic Design ディレクトリ:
```
frontend/src/components/
├── atoms/       最小UI部品（Button, Badge, Skeleton はshadcn/uiから直接import）
├── molecules/   atoms の組み合わせ（JudgmentBadge, ScanLimitBadge, AllergenToggle）
├── organisms/   機能ブロック（ScanCamera, ResultCard, HistoryCard, Navbar）
└── templates/   レイアウト枠（AppLayout, AuthLayout, AdminLayout）
```

- [ ] **Step 1: 失敗するテストを書く**

`frontend/src/components/atoms/LoadingOverlay.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { LoadingOverlay } from './LoadingOverlay'

describe('LoadingOverlay', () => {
  it('should show overlay when isOpen is true', () => {
    render(<LoadingOverlay isOpen={true} message="読み込み中" />)
    expect(screen.getByText('読み込み中')).toBeInTheDocument()
  })

  it('should not show overlay when isOpen is false', () => {
    render(<LoadingOverlay isOpen={false} message="読み込み中" />)
    expect(screen.queryByText('読み込み中')).not.toBeInTheDocument()
  })
})
```

`frontend/src/components/molecules/JudgmentBadge.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { JudgmentBadge } from './JudgmentBadge'

describe('JudgmentBadge', () => {
  it('renders ng as 🔴 NG', () => {
    render(<JudgmentBadge judgment="ng" />)
    expect(screen.getByText(/NG/)).toBeInTheDocument()
  })

  it('renders partial as 🟡 注意', () => {
    render(<JudgmentBadge judgment="partial" />)
    expect(screen.getByText(/注意/)).toBeInTheDocument()
  })

  it('renders ok as ✅ なし', () => {
    render(<JudgmentBadge judgment="ok" />)
    expect(screen.getByText(/なし/)).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
pnpm --filter frontend test LoadingOverlay
```

Expected: FAIL

- [ ] **Step 3: atoms を実装する**

`frontend/src/components/atoms/LoadingSpinner.tsx`:

```tsx
export const LoadingSpinner = ({ size = 'md' }: { size?: 'sm' | 'md' | 'lg' }) => {
  const sizeClass = { sm: 'h-4 w-4', md: 'h-8 w-8', lg: 'h-12 w-12' }[size]
  return (
    <div
      className={`${sizeClass} animate-spin rounded-full border-4 border-muted border-t-primary`}
      role="status"
    />
  )
}
```

`frontend/src/components/atoms/LoadingOverlay.tsx`:

```tsx
import { LoadingSpinner } from './LoadingSpinner'

type Props = {
  isOpen: boolean
  message?: string
}

/**
 * 全画面統一ローディングオーバーレイ。
 * 個別コンポーネントに独自ローディングを書かず必ずこれを使う。
 */
export const LoadingOverlay = ({ isOpen, message }: Props) => {
  if (!isOpen) return null
  return (
    <div className="fixed inset-0 z-50 flex flex-col items-center justify-center bg-background/80 backdrop-blur-sm">
      <LoadingSpinner size="lg" />
      {message && <p className="mt-4 text-sm text-muted-foreground">{message}</p>}
    </div>
  )
}
```

- [ ] **Step 4: molecules を実装する**

`frontend/src/components/molecules/JudgmentBadge.tsx`:

```tsx
import type { JudgmentShort } from '@/app/scan/scan.types'

type Props = {
  judgment: JudgmentShort
  className?: string
}

const JUDGMENT_CONFIG: Record<JudgmentShort, { label: string; className: string }> = {
  ng:      { label: '🔴 NG',   className: 'bg-destructive/10 text-destructive border-destructive/20' },
  partial: { label: '🟡 注意', className: 'bg-yellow-50 text-yellow-800 border-yellow-200' },
  ok:      { label: '✅ なし', className: 'bg-green-50 text-green-800 border-green-200' },
}

export const JudgmentBadge = ({ judgment, className = '' }: Props) => {
  const config = JUDGMENT_CONFIG[judgment]
  return (
    <span
      className={`inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-semibold ${config.className} ${className}`}
    >
      {config.label}
    </span>
  )
}
```

`frontend/src/components/molecules/ScanLimitBadge.tsx`:

```tsx
type Props = {
  used: number
  limit: number
}

export const ScanLimitBadge = ({ used, limit }: Props) => {
  const isNearLimit = used >= limit * 0.8
  const isAtLimit = used >= limit
  return (
    <span
      className={`text-xs font-medium ${isAtLimit ? 'text-destructive' : isNearLimit ? 'text-yellow-600' : 'text-muted-foreground'}`}
    >
      {used}/{limit}
    </span>
  )
}
```

- [ ] **Step 5: テストをパスすることを確認**

```bash
pnpm --filter frontend test atoms molecules
```

Expected: PASS

- [ ] **Step 6: コミット**

```bash
git add frontend/src/components/
git commit -m "feat(frontend): add Atomic Design structure, LoadingOverlay, JudgmentBadge atoms"
```

---

### Task 3: useAuth Hook + Supabase Auth 連携

**Files:**
- Create: `frontend/src/hooks/useAuth.ts`
- Create: `frontend/src/hooks/useAuth.test.ts`
- Create: `frontend/src/providers/AuthProvider.tsx`

- [ ] **Step 1: 失敗するテストを書く**

`frontend/src/hooks/useAuth.test.ts`:

```typescript
import { renderHook, act } from '@testing-library/react'
import { useAuth } from './useAuth'

jest.mock('@/lib/supabase/client', () => ({
  createClient: () => ({
    auth: {
      getSession: jest.fn().mockResolvedValue({ data: { session: null } }),
      onAuthStateChange: jest.fn().mockReturnValue({ data: { subscription: { unsubscribe: jest.fn() } } }),
    },
  }),
}))

describe('useAuth', () => {
  it('returns null session initially', () => {
    const wrapper = ({ children }: { children: React.ReactNode }) => children
    const { result } = renderHook(() => useAuth(), { wrapper })
    expect(result.current.session).toBeNull()
    expect(result.current.isLoading).toBe(true)
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
pnpm --filter frontend test useAuth
```

Expected: FAIL

- [ ] **Step 3: useAuth を実装する**

`frontend/src/hooks/useAuth.ts`:

```typescript
'use client'

import { useEffect, useState } from 'react'
import type { Session, User } from '@supabase/supabase-js'
import { createClient } from '@/lib/supabase/client'

type UseAuthReturn = {
  session: Session | null
  user: User | null
  isLoading: boolean
  signOut: () => Promise<void>
}

export const useAuth = (): UseAuthReturn => {
  const [session, setSession] = useState<Session | null>(null)
  const [isLoading, setIsLoading] = useState(true)
  const supabase = createClient()

  useEffect(() => {
    supabase.auth.getSession().then(({ data }) => {
      setSession(data.session)
      setIsLoading(false)
    })

    const { data: { subscription } } = supabase.auth.onAuthStateChange((_, newSession) => {
      setSession(newSession)
      setIsLoading(false)
    })

    return () => subscription.unsubscribe()
  }, [])

  const signOut = async () => {
    await supabase.auth.signOut()
  }

  return {
    session,
    user: session?.user ?? null,
    isLoading,
    signOut,
  }
}
```

- [ ] **Step 4: AuthProvider を作成する**

`frontend/src/providers/AuthProvider.tsx`:

```tsx
'use client'

import { createContext, useContext } from 'react'
import type { Session, User } from '@supabase/supabase-js'
import { useAuth } from '@/hooks/useAuth'

type AuthContextValue = {
  session: Session | null
  user: User | null
  isLoading: boolean
  signOut: () => Promise<void>
}

const AuthContext = createContext<AuthContextValue | null>(null)

export const AuthProvider = ({ children }: { children: React.ReactNode }) => {
  const auth = useAuth()
  return <AuthContext.Provider value={auth}>{children}</AuthContext.Provider>
}

export const useAuthContext = (): AuthContextValue => {
  const ctx = useContext(AuthContext)
  if (!ctx) throw new Error('useAuthContext must be used within AuthProvider')
  return ctx
}
```

- [ ] **Step 5: テストをパスすることを確認**

```bash
pnpm --filter frontend test useAuth
```

Expected: PASS

- [ ] **Step 6: コミット**

```bash
git add frontend/src/hooks/useAuth.ts frontend/src/providers/AuthProvider.tsx
git commit -m "feat(auth): add useAuth hook and AuthProvider"
```

---

### Task 4: Next.js Middleware 更新（認証ガード + リダイレクト）

**Files:**
- Modify: `frontend/src/middleware.ts`

仕様:
- 未ログインで `(app)/*`（/scan, /history, /settings）へアクセス → `/login?redirect=<元のURL>`
- 未ログインで `/admin/*` → `/403`
- `/public/history`・`/login`・`/signup`・`/reset-password` は認証不要
- ログイン済みで `/login`・`/signup` → `/scan` へリダイレクト

- [ ] **Step 1: middleware.ts を更新する**

`frontend/src/middleware.ts`:

```typescript
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'
import { createServerClient } from '@supabase/ssr'

const PUBLIC_PATHS = ['/login', '/signup', '/reset-password', '/public']
const AUTH_PATHS = ['/login', '/signup']

export async function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl

  // Supabase セッションを確認するためのクライアント（Cookie ベース）
  let response = NextResponse.next({ request })
  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll: () => request.cookies.getAll(),
        setAll: (cookiesToSet) => {
          cookiesToSet.forEach(({ name, value }) => request.cookies.set(name, value))
          response = NextResponse.next({ request })
          cookiesToSet.forEach(({ name, value, options }) =>
            response.cookies.set(name, value, options),
          )
        },
      },
    },
  )

  const { data: { user } } = await supabase.auth.getUser()

  const isPublicPath = PUBLIC_PATHS.some(p => pathname.startsWith(p))
  const isAuthPath = AUTH_PATHS.some(p => pathname === p)
  const isAdminPath = pathname.startsWith('/admin')

  // 未ログイン + 保護ルート → ログインページへ
  if (!user && !isPublicPath) {
    const loginUrl = new URL('/login', request.url)
    loginUrl.searchParams.set('redirect', pathname)
    return NextResponse.redirect(loginUrl)
  }

  // 未ログイン + /admin → 403
  if (!user && isAdminPath) {
    return NextResponse.redirect(new URL('/403', request.url))
  }

  // ログイン済み + /admin で admin でない → 403
  if (user && isAdminPath) {
    const role = (user.app_metadata as { role?: string })?.role
    if (role !== 'admin') {
      return NextResponse.redirect(new URL('/403', request.url))
    }
  }

  // ログイン済みで /login・/signup → /scan へ
  if (user && isAuthPath) {
    return NextResponse.redirect(new URL('/scan', request.url))
  }

  return response
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|ico)$).*)'],
}
```

- [ ] **Step 2: 型チェック**

```bash
pnpm --filter frontend typecheck
```

- [ ] **Step 3: コミット**

```bash
git add frontend/src/middleware.ts
git commit -m "feat(middleware): add Supabase auth guard with redirect"
```

---

### Task 5: Templates（AppLayout・AuthLayout）

**Files:**
- Create: `frontend/src/components/templates/AppLayout.tsx`
- Create: `frontend/src/components/templates/AuthLayout.tsx`
- Create: `frontend/src/components/organisms/Navbar.tsx`
- Modify: `frontend/src/app/layout.tsx`（AuthProvider + QueryClientProvider ラップ）

- [ ] **Step 1: AuthLayout を作成する**

`frontend/src/components/templates/AuthLayout.tsx`:

```tsx
type Props = { children: React.ReactNode }

export const AuthLayout = ({ children }: Props) => (
  <div className="flex min-h-screen items-center justify-center bg-background p-4">
    <div className="w-full max-w-md">
      <div className="mb-8 text-center">
        <h1 className="text-2xl font-bold">🥗 アレルギースキャン</h1>
        <p className="mt-1 text-sm text-muted-foreground">食品アレルギーを即座に確認</p>
      </div>
      {children}
    </div>
  </div>
)
```

- [ ] **Step 2: Navbar organism を作成する（レスポンシブ）**

`frontend/src/components/organisms/Navbar.tsx`:

```tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { useTranslations } from 'next-intl'
import { useAuthContext } from '@/providers/AuthProvider'

type NavItem = { href: string; labelKey: string; icon: string }

const NAV_ITEMS: NavItem[] = [
  { href: '/scan',    labelKey: 'nav.scan',    icon: '📷' },
  { href: '/history', labelKey: 'nav.history', icon: '📋' },
  { href: '/settings',labelKey: 'nav.settings',icon: '⚙️' },
]

export const Navbar = () => {
  const t = useTranslations('common')
  const pathname = usePathname()
  const { user, signOut } = useAuthContext()

  return (
    <>
      {/* PC: 左サイドナビ（hidden on SP/Tablet） */}
      <aside className="hidden lg:flex flex-col w-60 min-h-screen border-r bg-background p-4">
        <div className="mb-8">
          <span className="text-lg font-bold">🥗 アレルギースキャン</span>
        </div>
        <nav className="flex flex-col gap-1">
          {NAV_ITEMS.map(item => (
            <Link
              key={item.href}
              href={item.href}
              className={`flex items-center gap-3 rounded-lg px-3 py-2 text-sm transition-colors ${pathname === item.href ? 'bg-accent text-accent-foreground font-medium' : 'hover:bg-accent/50'}`}
            >
              <span>{item.icon}</span>
              <span>{t(item.labelKey)}</span>
            </Link>
          ))}
        </nav>
        <div className="mt-auto">
          <p className="mb-2 text-xs text-muted-foreground truncate">{user?.email}</p>
          <button onClick={signOut} className="w-full text-left text-sm text-muted-foreground hover:text-foreground">
            {t('nav.signOut')}
          </button>
        </div>
      </aside>

      {/* SP/Tablet: ボトムナビ（hidden on PC） */}
      <nav className="lg:hidden fixed bottom-0 left-0 right-0 z-40 flex border-t bg-background">
        {NAV_ITEMS.map(item => (
          <Link
            key={item.href}
            href={item.href}
            className={`flex flex-1 flex-col items-center gap-1 py-2 text-xs transition-colors ${pathname === item.href ? 'text-primary font-medium' : 'text-muted-foreground'}`}
          >
            <span className="text-xl">{item.icon}</span>
            <span>{t(item.labelKey)}</span>
          </Link>
        ))}
      </nav>
    </>
  )
}
```

- [ ] **Step 3: AppLayout を作成する**

`frontend/src/components/templates/AppLayout.tsx`:

```tsx
import { Navbar } from '@/components/organisms/Navbar'

type Props = { children: React.ReactNode }

export const AppLayout = ({ children }: Props) => (
  <div className="flex min-h-screen">
    <Navbar />
    <main className="flex-1 pb-16 lg:pb-0">
      {children}
    </main>
  </div>
)
```

- [ ] **Step 4: root layout.tsx を更新する（Providers ラップ）**

`frontend/src/app/layout.tsx` を更新（既存の next-intl 設定は維持しつつ以下を追加）:

```tsx
import { QueryClientProvider } from '@tanstack/react-query'
import { createQueryClient } from '@/lib/queryClient'
import { AuthProvider } from '@/providers/AuthProvider'

// 既存の RootLayout に追加
const queryClient = createQueryClient()

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    // 既存の html/body タグ内に以下を追加
    <QueryClientProvider client={queryClient}>
      <AuthProvider>
        {children}
      </AuthProvider>
    </QueryClientProvider>
  )
}
```

- [ ] **Step 5: 型チェック**

```bash
pnpm --filter frontend typecheck
```

- [ ] **Step 6: コミット**

```bash
git add frontend/src/components/templates/ frontend/src/components/organisms/Navbar.tsx frontend/src/app/layout.tsx
git commit -m "feat(frontend): add AppLayout, AuthLayout templates and responsive Navbar"
```

---

### Task 6: Auth ページ（Login・Signup・Reset-password）

**Files:**
- Create: `frontend/src/app/(auth)/login/page.tsx`
- Create: `frontend/src/app/(auth)/signup/page.tsx`
- Create: `frontend/src/app/(auth)/reset-password/page.tsx`
- Modify: `frontend/src/app/(auth)/layout.tsx`（AuthLayout 適用）

- [ ] **Step 1: (auth) レイアウトを作成する**

`frontend/src/app/(auth)/layout.tsx`:

```tsx
import { AuthLayout } from '@/components/templates/AuthLayout'

export default function AuthGroupLayout({ children }: { children: React.ReactNode }) {
  return <AuthLayout>{children}</AuthLayout>
}
```

- [ ] **Step 2: Login ページを作成する**

`frontend/src/app/(auth)/login/page.tsx`:

```tsx
'use client'

import { useState } from 'react'
import { useRouter, useSearchParams } from 'next/navigation'
import Link from 'next/link'
import { useTranslations } from 'next-intl'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { LoadingOverlay } from '@/components/atoms/LoadingOverlay'
import { createClient } from '@/lib/supabase/client'
import { initUser } from '@/lib/api/users.api'

export default function LoginPage() {
  const t = useTranslations('auth')
  const router = useRouter()
  const searchParams = useSearchParams()
  const redirect = searchParams.get('redirect') ?? '/scan'

  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [error, setError] = useState<string | null>(null)
  const [isPending, setIsPending] = useState(false)

  const supabase = createClient()

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    setError(null)
    setIsPending(true)
    try {
      const { error: signInError } = await supabase.auth.signInWithPassword({ email, password })
      if (signInError) {
        setError(t('error.invalidCredentials'))
        return
      }
      // 初回ログイン後のユーザーレコード作成（冪等）
      await initUser()
      router.push(redirect)
    } catch {
      setError(t('error.unknown'))
    } finally {
      setIsPending(false)
    }
  }

  const handleGoogleLogin = async () => {
    await supabase.auth.signInWithOAuth({
      provider: 'google',
      options: { redirectTo: `${window.location.origin}/auth/callback?redirect=${redirect}` },
    })
  }

  return (
    <div className="space-y-6">
      <LoadingOverlay isOpen={isPending} message={t('loading.signingIn')} />

      <form onSubmit={handleSubmit} className="space-y-4">
        <div className="space-y-2">
          <Label htmlFor="email">{t('label.email')}</Label>
          <Input
            id="email"
            type="email"
            value={email}
            onChange={e => setEmail(e.target.value)}
            required
            autoComplete="email"
          />
        </div>
        <div className="space-y-2">
          <Label htmlFor="password">{t('label.password')}</Label>
          <Input
            id="password"
            type="password"
            value={password}
            onChange={e => setPassword(e.target.value)}
            required
            autoComplete="current-password"
          />
        </div>
        {error && <p className="text-sm text-destructive">{error}</p>}
        <Button type="submit" className="w-full" disabled={isPending}>
          {t('button.signIn')}
        </Button>
      </form>

      <div className="relative">
        <div className="absolute inset-0 flex items-center"><span className="w-full border-t" /></div>
        <div className="relative flex justify-center text-xs uppercase">
          <span className="bg-background px-2 text-muted-foreground">{t('label.or')}</span>
        </div>
      </div>

      <Button variant="outline" className="w-full" onClick={handleGoogleLogin}>
        {t('button.signInWithGoogle')}
      </Button>

      <div className="text-center text-sm space-y-1">
        <p>
          <Link href="/reset-password" className="text-primary hover:underline">
            {t('link.forgotPassword')}
          </Link>
        </p>
        <p>
          {t('text.noAccount')}{' '}
          <Link href="/signup" className="text-primary hover:underline">
            {t('link.signUp')}
          </Link>
        </p>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Signup ページを作成する**

`frontend/src/app/(auth)/signup/page.tsx`:

```tsx
'use client'

import { useState } from 'react'
import { useTranslations } from 'next-intl'
import Link from 'next/link'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { LoadingOverlay } from '@/components/atoms/LoadingOverlay'
import { createClient } from '@/lib/supabase/client'

export default function SignupPage() {
  const t = useTranslations('auth')
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [done, setDone] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const [isPending, setIsPending] = useState(false)
  const supabase = createClient()

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    setError(null)
    setIsPending(true)
    try {
      const { error: signUpError } = await supabase.auth.signUp({
        email,
        password,
        options: { emailRedirectTo: `${window.location.origin}/auth/callback` },
      })
      if (signUpError) {
        setError(signUpError.message)
        return
      }
      setDone(true)
    } finally {
      setIsPending(false)
    }
  }

  if (done) {
    return (
      <div className="text-center space-y-4">
        <p className="text-2xl">📧</p>
        <h2 className="font-bold">{t('signup.confirmEmailTitle')}</h2>
        <p className="text-sm text-muted-foreground">{t('signup.confirmEmailBody', { email })}</p>
      </div>
    )
  }

  return (
    <div className="space-y-6">
      <LoadingOverlay isOpen={isPending} message={t('loading.signingUp')} />
      <form onSubmit={handleSubmit} className="space-y-4">
        <div className="space-y-2">
          <Label htmlFor="email">{t('label.email')}</Label>
          <Input id="email" type="email" value={email} onChange={e => setEmail(e.target.value)} required />
        </div>
        <div className="space-y-2">
          <Label htmlFor="password">{t('label.password')}</Label>
          <Input id="password" type="password" value={password} onChange={e => setPassword(e.target.value)} required minLength={8} />
        </div>
        {error && <p className="text-sm text-destructive">{error}</p>}
        <Button type="submit" className="w-full" disabled={isPending}>{t('button.signUp')}</Button>
      </form>
      <p className="text-center text-sm">
        {t('text.haveAccount')}{' '}
        <Link href="/login" className="text-primary hover:underline">{t('link.signIn')}</Link>
      </p>
    </div>
  )
}
```

- [ ] **Step 4: Reset-password ページを作成する**

`frontend/src/app/(auth)/reset-password/page.tsx`:

```tsx
'use client'

import { useState } from 'react'
import { useTranslations } from 'next-intl'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { createClient } from '@/lib/supabase/client'

export default function ResetPasswordPage() {
  const t = useTranslations('auth')
  const [email, setEmail] = useState('')
  const [done, setDone] = useState(false)
  const [isPending, setIsPending] = useState(false)
  const supabase = createClient()

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    setIsPending(true)
    await supabase.auth.resetPasswordForEmail(email, {
      redirectTo: `${window.location.origin}/auth/update-password`,
    })
    setDone(true)
    setIsPending(false)
  }

  if (done) {
    return (
      <div className="text-center space-y-4">
        <p className="text-2xl">📧</p>
        <h2 className="font-bold">{t('resetPassword.sentTitle')}</h2>
        <p className="text-sm text-muted-foreground">{t('resetPassword.sentBody', { email })}</p>
      </div>
    )
  }

  return (
    <form onSubmit={handleSubmit} className="space-y-4">
      <p className="text-sm text-muted-foreground">{t('resetPassword.description')}</p>
      <div className="space-y-2">
        <Label htmlFor="email">{t('label.email')}</Label>
        <Input id="email" type="email" value={email} onChange={e => setEmail(e.target.value)} required />
      </div>
      <Button type="submit" className="w-full" disabled={isPending}>{t('button.sendResetLink')}</Button>
    </form>
  )
}
```

- [ ] **Step 5: OAuth コールバックルートを作成する**

`frontend/src/app/auth/callback/route.ts`:

```typescript
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'

export async function GET(request: NextRequest) {
  const { searchParams } = new URL(request.url)
  const code = searchParams.get('code')
  const redirect = searchParams.get('redirect') ?? '/scan'

  if (code) {
    const cookieStore = await cookies()
    const supabase = createServerClient(
      process.env.NEXT_PUBLIC_SUPABASE_URL!,
      process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
      {
        cookies: {
          getAll: () => cookieStore.getAll(),
          setAll: (cookiesToSet) => {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options),
            )
          },
        },
      },
    )
    await supabase.auth.exchangeCodeForSession(code)
  }

  return NextResponse.redirect(new URL(redirect, request.url))
}
```

- [ ] **Step 6: i18n キーを追加**

`frontend/public/locales/ja/auth.json`（新規作成）:

```json
{
  "label": { "email": "メールアドレス", "password": "パスワード", "or": "または" },
  "button": {
    "signIn": "ログイン",
    "signUp": "アカウント作成",
    "signInWithGoogle": "Googleでログイン",
    "sendResetLink": "リセットリンクを送信"
  },
  "link": {
    "forgotPassword": "パスワードを忘れた方",
    "signUp": "アカウント作成",
    "signIn": "ログイン"
  },
  "text": {
    "noAccount": "アカウントをお持ちでない方は",
    "haveAccount": "すでにアカウントをお持ちの方は"
  },
  "error": {
    "invalidCredentials": "メールアドレスまたはパスワードが正しくありません",
    "unknown": "エラーが発生しました。もう一度お試しください"
  },
  "loading": { "signingIn": "ログイン中...", "signingUp": "アカウント作成中..." },
  "signup": {
    "confirmEmailTitle": "確認メールを送信しました",
    "confirmEmailBody": "{email} に確認リンクを送信しました"
  },
  "resetPassword": {
    "description": "登録したメールアドレスを入力してください。パスワードリセットリンクを送信します。",
    "sentTitle": "メールを送信しました",
    "sentBody": "{email} にリセットリンクを送信しました"
  }
}
```

`frontend/public/locales/en/auth.json`（英語版も同様に作成）:

```json
{
  "label": { "email": "Email", "password": "Password", "or": "or" },
  "button": {
    "signIn": "Sign In",
    "signUp": "Create Account",
    "signInWithGoogle": "Sign in with Google",
    "sendResetLink": "Send Reset Link"
  },
  "link": { "forgotPassword": "Forgot password?", "signUp": "Create account", "signIn": "Sign in" },
  "text": { "noAccount": "Don't have an account?", "haveAccount": "Already have an account?" },
  "error": {
    "invalidCredentials": "Invalid email or password",
    "unknown": "An error occurred. Please try again."
  },
  "loading": { "signingIn": "Signing in...", "signingUp": "Creating account..." },
  "signup": {
    "confirmEmailTitle": "Check your email",
    "confirmEmailBody": "We sent a confirmation link to {email}"
  },
  "resetPassword": {
    "description": "Enter your email address and we'll send you a reset link.",
    "sentTitle": "Email sent",
    "sentBody": "We sent a reset link to {email}"
  }
}
```

- [ ] **Step 7: 型チェック**

```bash
pnpm --filter frontend typecheck
```

- [ ] **Step 8: コミット**

```bash
git add frontend/src/app/ frontend/public/locales/
git commit -m "feat(auth): add login, signup, reset-password pages with Supabase Auth"
```

---

### Task 7: useScan 刷新（タップ撮影・プレビュー確認）

**Files:**
- Modify: `frontend/src/app/scan/scan.types.ts`
- Modify: `frontend/src/hooks/useScan.ts`
- Modify: `frontend/src/hooks/useScan.spec.ts`

新しい ScanState:
- `idle` — カメラ起動・待機
- `preview` — 撮影画像表示中（撮り直し / この画像を使う）
- `processing` — API 送信中
- `result` — 結果表示
- `error` — エラー

削除: `detecting`・`stable`（自動撮影は廃止）

- [ ] **Step 1: scan.types.ts を更新する**

`frontend/src/app/scan/scan.types.ts` の `ScanState` を変更:

```typescript
export type ScanState =
  | 'idle'        // カメラ起動・待機（撮影ボタン有効）
  | 'preview'     // 撮影プレビュー表示（撮り直し / 確定）
  | 'processing'  // API 送信中
  | 'result'      // 結果表示
  | 'error'       // エラー
```

`ScanError` に追加:

```typescript
export type ScanError =
  | 'quality_check_failed'  // 明るさ・ぼやけ・テキスト領域NG
  | 'incomplete'
  | 'confidence_low'
  | 'api_error'
  | 'daily_limit_exceeded'  // 日次上限超過（429）
  | 'cooldown'              // 3秒クールダウン中
```

新しい Action に `preview` 関連を追加:

```typescript
export type Action =
  | { type: 'START_CAMERA' }
  | { type: 'PREVIEW'; imageDataUrl: string }  // 撮影プレビュー表示
  | { type: 'PROCESSING' }
  | { type: 'RESULT'; payload: ScanResult }
  | { type: 'ERROR'; error: ScanError }
  | { type: 'RESET' }

export type State = {
  scanState: ScanState
  error: ScanError | null
  result: ScanResult | null
  previewDataUrl: string | null  // プレビュー表示用 data URL
  storeCandidates: StoreCandidate[]
}
```

- [ ] **Step 2: 失敗するテストを書く**

`frontend/src/hooks/useScan.spec.ts` の既存テストを更新（`detecting`・`stable` 状態のテストを削除し、タップ撮影テストを追加）:

```typescript
describe('scanReducer', () => {
  it('START_CAMERA → idle with error/result cleared', () => {
    const state = scanReducer(
      { scanState: 'error', error: 'api_error', result: null, previewDataUrl: null, storeCandidates: [] },
      { type: 'START_CAMERA' },
    )
    expect(state.scanState).toBe('idle')
    expect(state.error).toBeNull()
  })

  it('PREVIEW → preview state with imageDataUrl', () => {
    const state = scanReducer(
      { ...initialState },
      { type: 'PREVIEW', imageDataUrl: 'data:image/jpeg;base64,...' },
    )
    expect(state.scanState).toBe('preview')
    expect(state.previewDataUrl).toBe('data:image/jpeg;base64,...')
  })

  it('ERROR daily_limit_exceeded → error state', () => {
    const state = scanReducer(initialState, { type: 'ERROR', error: 'daily_limit_exceeded' })
    expect(state.scanState).toBe('error')
    expect(state.error).toBe('daily_limit_exceeded')
  })
})
```

- [ ] **Step 3: useScan の reducer を更新する**

`frontend/src/hooks/useScan.ts` の `scanReducer` を更新:

```typescript
export const scanReducer = (state: State, action: Action): State => {
  switch (action.type) {
    case 'START_CAMERA':
      return { ...initialState, scanState: 'idle' }

    case 'PREVIEW':
      return { ...state, scanState: 'preview', previewDataUrl: action.imageDataUrl, error: null }

    case 'PROCESSING':
      return { ...state, scanState: 'processing', previewDataUrl: null }

    case 'RESULT': {
      const storeCandidates = action.payload.type === 'ocr' ? (action.payload.storeCandidates ?? []) : []
      return { ...state, scanState: 'result', result: action.payload, storeCandidates }
    }

    case 'ERROR':
      return { ...state, scanState: 'error', error: action.error }

    case 'RESET':
      return initialState

    default:
      return state
  }
}
```

- [ ] **Step 4: useScan の主要ロジックを更新する**

`useScan` から frame check ループを削除し、以下の関数を追加:

```typescript
const SCAN_COOLDOWN_MS = 3_000
const lastScanTimestampRef = useRef<number>(0)

/**
 * 撮影ボタン押下ハンドラ（タップ撮影統一フロー）。
 * 1. クールダウンチェック
 * 2. フレームキャプチャ
 * 3. ZXing 静止画バーコード検出（~50ms）
 *    ├── 検出 → /scan/barcode へ送信
 *    └── 未検出 → プレチェック → preview 状態へ遷移
 */
const handleCapture = useCallback(async (): Promise<void> => {
  const elapsed = Date.now() - lastScanTimestampRef.current
  if (elapsed < SCAN_COOLDOWN_MS) {
    dispatch({ type: 'ERROR', error: 'cooldown' })
    return
  }

  const imageData = captureFrame()
  if (!imageData) return

  // ZXing 静止画バーコード検出（向き自動試行）
  const barcodeResult = await detectFromImageData(imageData)
  if (barcodeResult) {
    lastScanTimestampRef.current = Date.now()
    dispatch({ type: 'PROCESSING' })
    try {
      const res = await scanBarcodeWithCache(barcodeResult)
      dispatch({ type: 'RESULT', payload: { type: 'barcode', data: res } })
      // 履歴保存
      const historyBody = buildHistoryBody({ type: 'barcode', data: res })
      if (historyBody) await saveHistory(historyBody)
    } catch (err: unknown) {
      const status = (err as { status?: number }).status
      if (status === 429) dispatch({ type: 'ERROR', error: 'daily_limit_exceeded' })
      else dispatch({ type: 'ERROR', error: 'api_error' })
    }
    return
  }

  // バーコード未検出 → プレチェック（明るさ・シャープネス・テキスト領域）
  if (!isQualityOk(imageData)) {
    dispatch({ type: 'ERROR', error: 'quality_check_failed' })
    return
  }

  // プレビュー表示（ユーザーが確認）
  const canvas = document.createElement('canvas')
  canvas.width = imageData.width
  canvas.height = imageData.height
  canvas.getContext('2d')!.putImageData(imageData, 0, 0)
  const dataUrl = canvas.toDataURL('image/jpeg', 0.85)
  dispatch({ type: 'PREVIEW', imageDataUrl: dataUrl })
}, [captureFrame, detectFromImageData, isQualityOk, scanBarcodeWithCache, saveHistory])

/**
 * プレビュー確認後の OCR 送信ハンドラ。
 * capturedImageData が ref で保持されているものを使用する。
 */
const confirmAndScan = useCallback(async (): Promise<void> => {
  const imageData = capturedImageDataRef.current
  if (!imageData) return
  lastScanTimestampRef.current = Date.now()
  await runOcrFlow(imageData)
}, [runOcrFlow])
```

`runOcrFlow` は前処理として EXIF 補正を追加（モバイル撮影向け。既存の前処理に追加）。

`UseScanReturn` に `handleCapture` と `confirmAndScan` を追加し、`manualCapture` は削除する。

- [ ] **Step 5: テストをパスすることを確認**

```bash
pnpm --filter frontend test useScan
```

Expected: PASS

- [ ] **Step 6: 型チェック**

```bash
pnpm --filter frontend typecheck
```

- [ ] **Step 7: コミット**

```bash
git add frontend/src/hooks/useScan.ts frontend/src/app/scan/scan.types.ts
git commit -m "feat(scan): refactor useScan to tap-to-capture with preview state"
```

---

### Task 8: Scan ページ更新（タップ撮影 UI）

**Files:**
- Modify: `frontend/src/app/scan/page.tsx`
- Modify: `frontend/src/components/ResultCard.tsx` → `frontend/src/components/organisms/ResultCard.tsx`

- [ ] **Step 1: ResultCard を organisms 配下に移動（または新規作成）**

既存の `frontend/src/components/ResultCard.tsx` を `frontend/src/components/organisms/ResultCard.tsx` に移動。インポートパスを更新する。

- [ ] **Step 2: Scan ページを更新する**

`frontend/src/app/scan/page.tsx` を以下に置き換え（主要な変更点のみ示す）:

```tsx
'use client'

import { useEffect, useState } from 'react'
import { useTranslations } from 'next-intl'
import { AppLayout } from '@/components/templates/AppLayout'
import { LoadingOverlay } from '@/components/atoms/LoadingOverlay'
import { ScanLimitBadge } from '@/components/molecules/ScanLimitBadge'
import { useScan } from '@/hooks/useScan'
import { useAuthContext } from '@/providers/AuthProvider'
import { ResultCard } from '@/components/organisms/ResultCard'
import { getUser } from '@/lib/api/users.api'

export default function ScanPage() {
  const t = useTranslations('scan')
  const { user } = useAuthContext()
  const [scanUsage, setScanUsage] = useState<{ used: number; limit: number } | null>(null)

  const {
    scanState,
    error,
    result,
    previewDataUrl,
    videoRef,
    startScan,
    stopScan,
    reset,
    handleCapture,
    confirmAndScan,
    zoomLevel,
    setZoom,
    facingMode,
    toggleFacingMode,
    cooldownRemainingMs,
  } = useScan()

  useEffect(() => {
    void startScan()
    return () => stopScan()
  }, [])

  useEffect(() => {
    if (!user) return
    getUser().then(u => {
      if (u.subscription) {
        // scan usage は getUser レスポンスから取得（MVP では残回数を別 API で取得しない）
        setScanUsage(null) // TODO: userDailyScans API を別途追加
      }
    })
  }, [user])

  const isOnCooldown = cooldownRemainingMs > 0

  // プレビュー画面
  if (scanState === 'preview' && previewDataUrl) {
    return (
      <AppLayout>
        <div className="flex flex-col items-center gap-4 p-4">
          <h2 className="font-bold">{t('preview.title')}</h2>
          <img src={previewDataUrl} alt="preview" className="max-w-sm rounded-lg shadow" />
          <div className="flex gap-4">
            <button
              onClick={reset}
              className="rounded-lg border px-6 py-3 text-sm font-medium hover:bg-accent"
            >
              {t('preview.retake')}
            </button>
            <button
              onClick={confirmAndScan}
              className="rounded-lg bg-primary px-6 py-3 text-sm font-medium text-primary-foreground"
            >
              {t('preview.confirm')}
            </button>
          </div>
        </div>
      </AppLayout>
    )
  }

  // 結果画面
  if (scanState === 'result' && result) {
    return (
      <AppLayout>
        <ResultCard result={result} onReset={reset} />
      </AppLayout>
    )
  }

  // カメラ画面（idle / processing / error）
  return (
    <AppLayout>
      <LoadingOverlay isOpen={scanState === 'processing'} message={t('processing')} />

      <div className="relative flex h-[calc(100dvh-4rem)] flex-col lg:h-screen">
        {/* カメラビュー */}
        <video
          ref={videoRef}
          autoPlay
          playsInline
          muted
          className="h-full w-full object-cover"
        />

        {/* オーバーレイUI */}
        <div className="absolute inset-0 flex flex-col justify-between p-4">
          {/* 上部: スキャン上限バッジ + カメラ切替 */}
          <div className="flex items-center justify-between">
            {scanUsage && (
              <ScanLimitBadge used={scanUsage.used} limit={scanUsage.limit} />
            )}
            <button onClick={toggleFacingMode} className="rounded-full bg-black/40 p-2 text-white lg:hidden">
              🔄
            </button>
          </div>

          {/* エラー表示 */}
          {error && (
            <div className="mx-auto rounded-lg bg-black/60 px-4 py-2 text-sm text-white">
              {t(`error.${error}`)}
            </div>
          )}

          {/* 下部: 撮影ボタン */}
          <div className="flex justify-center pb-8">
            <button
              onClick={handleCapture}
              disabled={isOnCooldown || scanState === 'processing'}
              className="h-20 w-20 rounded-full border-4 border-white bg-white/20 backdrop-blur-sm transition-opacity disabled:opacity-50"
            >
              {isOnCooldown ? (
                <span className="text-white text-sm font-bold">
                  {Math.ceil(cooldownRemainingMs / 1000)}s
                </span>
              ) : (
                <span className="text-2xl">📷</span>
              )}
            </button>
          </div>
        </div>
      </div>

      {/* 免責事項（常時表示） */}
      <p className="p-2 text-center text-xs text-muted-foreground">
        {t('caution')}
      </p>
    </AppLayout>
  )
}
```

- [ ] **Step 3: 型チェック**

```bash
pnpm --filter frontend typecheck
```

- [ ] **Step 4: コミット**

```bash
git add frontend/src/app/scan/ frontend/src/components/organisms/
git commit -m "feat(scan): update scan page for tap-to-capture UX"
```

---

### Task 9: HistoryCard 更新 + History ページ

**Files:**
- Modify: `frontend/src/components/HistoryCard.tsx` → `frontend/src/components/organisms/HistoryCard.tsx`
- Modify: `frontend/src/app/history/page.tsx`
- Modify: `frontend/src/hooks/useHistory.ts`
- Modify: `frontend/src/lib/api/history.api.ts`

- [ ] **Step 1: history.api.ts を更新する（apiFetch 使用 + is_public・thumbnail_url 対応）**

`frontend/src/lib/api/history.api.ts` の `patchHistory` 関数シグネチャを更新:

```typescript
export type PatchHistoryBody = {
  product_name?: string | null
  store_name?: string | null
  memo?: string | null
  is_public?: boolean
  thumbnail_url?: string | null
}
```

Cookie ベースの fetch を `apiFetch` に置き換え。

- [ ] **Step 2: history.types.ts を更新する**

`frontend/src/app/history/history.types.ts` の `HistoryItem` に追加:

```typescript
export type HistoryItem = {
  // ... 既存フィールド
  thumbnail_url: string | null
  ocr_image_url: string | null
  is_public: boolean
  memo: string | null
}
```

- [ ] **Step 3: HistoryCard を organisms に移動・更新**

既存 `frontend/src/components/HistoryCard.tsx` を `frontend/src/components/organisms/HistoryCard.tsx` に移動し、以下の機能を追加:
- サムネイル画像表示（`thumbnail_url` が null の場合はプレースホルダー）
- メモ表示
- `is_public` フラグ表示（公開/非公開バッジ）

```tsx
// 公開フラグ表示
{item.is_public ? (
  <span className="text-xs text-muted-foreground">🌐 公開中</span>
) : (
  <span className="text-xs text-muted-foreground">🔒 非公開</span>
)}

// サムネイル表示
{item.thumbnail_url ? (
  <img src={item.thumbnail_url} alt="" className="h-16 w-16 rounded object-cover" />
) : (
  <div className="h-16 w-16 rounded bg-muted" />
)}

// メモ表示
{item.memo && <p className="text-sm text-muted-foreground line-clamp-2">{item.memo}</p>}
```

- [ ] **Step 4: History ページを更新する**

`frontend/src/app/history/page.tsx` の主要な変更:
1. `useOnboardingGuard` を削除し `useAuthContext` を使用
2. `getUser()` の直接呼び出しを `useAuthContext().user` に変更
3. 編集フォームに `memo` と `is_public` フィールドを追加

```tsx
// useAuthContext から userId を取得（getUser() の直接呼び出しを置き換え）
const { user } = useAuthContext()
const userId = user?.id

// 編集フォームに memo と is_public を追加
type EditFormData = {
  product_name: string
  store_name: string
  memo: string
  is_public: boolean
}
```

- [ ] **Step 5: テスト**

```bash
pnpm --filter frontend test HistoryCard useHistory
```

- [ ] **Step 6: コミット**

```bash
git add frontend/src/components/organisms/HistoryCard.tsx \
        frontend/src/app/history/ \
        frontend/src/hooks/useHistory.ts \
        frontend/src/lib/api/history.api.ts
git commit -m "feat(history): update HistoryCard with thumbnail/memo/is_public support"
```

---

### Task 10: みんなの履歴ページ（Public History）

**Files:**
- Create: `frontend/src/app/public/history/page.tsx`
- Create: `frontend/src/hooks/usePublicHistory.ts`
- Modify: `frontend/src/lib/api/history.api.ts`

- [ ] **Step 1: public history API クライアントを追加する**

`frontend/src/lib/api/history.api.ts` に追加:

```typescript
export const getPublicHistory = async (limit: number, before?: string) => {
  // @Public() エンドポイント: Bearer Token 不要（apiFetch も送るが問題なし）
  const params = new URLSearchParams({ limit: String(limit), ...(before ? { before } : {}) })
  const res = await fetch(`${process.env.NEXT_PUBLIC_API_BASE_URL}/public/history?${params}`)
  return res.json() as Promise<{ items: PublicHistoryItem[]; next_before: string | null }>
}

export const getPublicHistoryDigest = async () => {
  const res = await fetch(`${process.env.NEXT_PUBLIC_API_BASE_URL}/public/history/digest`)
  return res.json() as Promise<{ count: number; last_updated_at: string | null }>
}

export type PublicHistoryItem = {
  id: string
  product_name: string | null
  judgment: 'ng' | 'partial' | 'ok'
  thumbnail_url: string | null
  store_name: string | null
  scanned_at: string
}
```

- [ ] **Step 2: usePublicHistory Hook を作成する**

`frontend/src/hooks/usePublicHistory.ts`:

```typescript
import { useInfiniteQuery, useQuery } from '@tanstack/react-query'
import { getPublicHistory, getPublicHistoryDigest } from '@/lib/api/history.api'

/** みんなの履歴無限スクロール（staleTime: Infinity でスクロール中の再取得を防ぐ）。 */
export const usePublicHistory = () =>
  useInfiniteQuery({
    queryKey: ['public-history'],
    queryFn: ({ pageParam }) => getPublicHistory(20, pageParam as string | undefined),
    initialPageParam: undefined as string | undefined,
    getNextPageParam: lastPage => lastPage.next_before ?? undefined,
    staleTime: Infinity,
    refetchOnWindowFocus: false,
    refetchOnReconnect: false,
  })

/** 60秒ごとに軽量ポーリングして新着を検知。 */
export const usePublicHistoryDigest = () =>
  useQuery({
    queryKey: ['public-history-digest'],
    queryFn: getPublicHistoryDigest,
    refetchInterval: 60 * 1000,
    staleTime: Infinity,
  })
```

- [ ] **Step 3: Public History ページを作成する**

`frontend/src/app/public/history/page.tsx`:

```tsx
'use client'

import { useEffect, useRef, useState } from 'react'
import { useTranslations } from 'next-intl'
import { JudgmentBadge } from '@/components/molecules/JudgmentBadge'
import { LoadingSpinner } from '@/components/atoms/LoadingSpinner'
import { usePublicHistory, usePublicHistoryDigest } from '@/hooks/usePublicHistory'
import { useQueryClient } from '@tanstack/react-query'
import type { JudgmentShort } from '@/app/scan/scan.types'

export default function PublicHistoryPage() {
  const t = useTranslations('history')
  const queryClient = useQueryClient()
  const [showUpdateBanner, setShowUpdateBanner] = useState(false)

  const {
    data,
    fetchNextPage,
    hasNextPage,
    isFetchingNextPage,
    isLoading,
  } = usePublicHistory()

  const { data: digest } = usePublicHistoryDigest()

  // 初回の digest 値を ref で保持し、変化があればバナー表示
  const initialDigestRef = useRef<{ count: number; last_updated_at: string | null } | null>(null)
  useEffect(() => {
    if (!digest) return
    if (!initialDigestRef.current) {
      initialDigestRef.current = digest
      return
    }
    const hasChange =
      digest.count !== initialDigestRef.current.count ||
      digest.last_updated_at !== initialDigestRef.current.last_updated_at
    if (hasChange) setShowUpdateBanner(true)
  }, [digest])

  const handleRefresh = () => {
    initialDigestRef.current = digest ?? null
    setShowUpdateBanner(false)
    void queryClient.invalidateQueries({ queryKey: ['public-history'] })
  }

  // 無限スクロール: 最下部の sentinel 要素を IntersectionObserver で監視
  const sentinelRef = useRef<HTMLDivElement>(null)
  useEffect(() => {
    if (!sentinelRef.current || !hasNextPage) return
    const observer = new IntersectionObserver(entries => {
      if (entries[0]?.isIntersecting && !isFetchingNextPage) void fetchNextPage()
    })
    observer.observe(sentinelRef.current)
    return () => observer.disconnect()
  }, [fetchNextPage, hasNextPage, isFetchingNextPage])

  const items = data?.pages.flatMap(page => page.items) ?? []

  return (
    <div className="mx-auto max-w-2xl p-4">
      <h1 className="mb-4 text-xl font-bold">{t('public.title')}</h1>

      {/* 新着バナー */}
      {showUpdateBanner && (
        <button
          onClick={handleRefresh}
          className="mb-4 w-full rounded-lg bg-primary/10 px-4 py-3 text-sm font-medium text-primary"
        >
          {t('public.newItems')} — {t('public.refresh')}
        </button>
      )}

      {isLoading ? (
        <div className="flex justify-center py-12"><LoadingSpinner /></div>
      ) : (
        <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
          {items.map(item => (
            <div key={item.id} className="rounded-lg border bg-card p-4 shadow-sm">
              {item.thumbnail_url && (
                <img src={item.thumbnail_url} alt="" className="mb-3 h-40 w-full rounded object-cover" />
              )}
              <div className="flex items-start justify-between gap-2">
                <p className="font-medium line-clamp-2">{item.product_name ?? t('unnamed')}</p>
                <JudgmentBadge judgment={item.judgment as JudgmentShort} />
              </div>
              {item.store_name && <p className="mt-1 text-xs text-muted-foreground">📍 {item.store_name}</p>}
              <p className="mt-1 text-xs text-muted-foreground">
                {new Date(item.scanned_at).toLocaleDateString('ja-JP')}
              </p>
            </div>
          ))}
        </div>
      )}

      {/* 無限スクロール sentinel */}
      <div ref={sentinelRef} className="py-4 flex justify-center">
        {isFetchingNextPage && <LoadingSpinner size="sm" />}
      </div>
    </div>
  )
}
```

- [ ] **Step 4: i18n キーを追加**

`frontend/public/locales/ja/history.json` に追加:

```json
{
  "public": {
    "title": "みんなの履歴",
    "newItems": "新しいスキャンがあります",
    "refresh": "更新する"
  },
  "unnamed": "商品名なし"
}
```

- [ ] **Step 5: コミット**

```bash
git add frontend/src/app/public/ frontend/src/hooks/usePublicHistory.ts
git commit -m "feat(history): add public history page with infinite scroll and digest polling"
```

---

### Task 11: Settings ページ更新（サブスク情報）

**Files:**
- Modify: `frontend/src/app/settings/page.tsx`
- Modify: `frontend/src/hooks/useSettings.ts`

変更点:
1. `useOnboardingGuard` 削除 → `useAuthContext` 使用
2. 現在のプラン・日次スキャン上限を表示
3. Google ログインユーザーにはパスワード変更 UI を非表示

- [ ] **Step 1: useSettings.ts を更新する**

`frontend/src/hooks/useSettings.ts` の `getUser()` 呼び出しを `apiFetch` ベースに更新し、レスポンスに `subscription` フィールドを追加する。

- [ ] **Step 2: Settings ページを更新する**

`frontend/src/app/settings/page.tsx` に以下を追加:

```tsx
// パスワード変更は email プロバイダーのみ表示
const { user } = useAuthContext()
const isEmailProvider = user?.identities?.some(identity => identity.provider === 'email') ?? false

// サブスク情報表示
{userSettings?.subscription && (
  <section className="rounded-lg border p-4">
    <h2 className="mb-2 font-bold">{t('settings.plan')}</h2>
    <p className="text-sm">{userSettings.subscription.plan_name}</p>
    <p className="text-xs text-muted-foreground">
      {t('settings.dailyLimit', { limit: userSettings.subscription.daily_scan_limit })}
    </p>
  </section>
)}

// パスワード変更（email プロバイダーのみ）
{isEmailProvider && (
  <section className="rounded-lg border p-4">
    <h2 className="mb-2 font-bold">{t('settings.changePassword')}</h2>
    {/* パスワード変更フォーム（supabase.auth.updateUser({ password }) を使用） */}
  </section>
)}
```

- [ ] **Step 3: コミット**

```bash
git add frontend/src/app/settings/
git commit -m "feat(settings): add subscription info and conditional password change"
```

---

### Task 12: 全体テスト + 型チェック確認

- [ ] **Step 1: フロントエンド全テスト**

```bash
pnpm --filter frontend test
```

Expected: 全テストパス

- [ ] **Step 2: 型チェック**

```bash
pnpm --filter frontend typecheck
```

Expected: エラーなし

- [ ] **Step 3: i18n キー漏れ確認**

```bash
# ja/en のキーが一致しているか確認
diff <(jq 'keys | .[]' frontend/public/locales/ja/auth.json | sort) \
     <(jq 'keys | .[]' frontend/public/locales/en/auth.json | sort)
```

Expected: 差分なし（全キーが ja/en 両方に存在する）

---

## 完了チェックリスト

- [ ] `pnpm --filter frontend test` 全パス
- [ ] `pnpm --filter frontend typecheck` エラーなし
- [ ] Login / Signup / Reset-password ページが動作する
- [ ] Middleware が未ログインを /login にリダイレクトする
- [ ] タップ撮影 → ZXing 検出 → バーコード/OCR 自動振り分けが動作する
- [ ] プレビュー表示 → 撮り直し / 確定 が動作する
- [ ] 日次スキャン上限超過で 429 エラーが表示される
- [ ] 3 秒クールダウン中は撮影ボタンが無効化される
- [ ] みんなの履歴が認証なしで表示される
- [ ] 新着バナーが 60 秒ポーリングで表示される
- [ ] LoadingOverlay が全画面統一で使用されている（独自ローディング禁止）
- [ ] UIテキストがすべて `t('キー名')` 経由で管理されている

## 次のステップ

- **Phase 3** → `.claude/plans/pending/2026-06-05-admin-subscription.md`（管理画面 + Stripe）
